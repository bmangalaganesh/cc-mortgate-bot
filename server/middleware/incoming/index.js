const commands = require('./commands');
const watsonContext = require('./watson_context');
const watsonUpdate = require('./watson_update');

module.exports = {
  commands,
  watsonContext,
  watsonUpdate,
};
